# - do the mapping - write a class that maps to tables

# - I got a table named Jobs - class name is Job

# class Jobs < means inherit from  ActiveRecord::Base
# once you inherit it gives you methods(all) (which means all tables)

class Application < ActiveRecord::Base


end